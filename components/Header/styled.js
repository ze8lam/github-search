import styled from "styled-components";

export const RadioContainer = styled.div`
  display: flex;
  margin-top: 1rem;
  
  label {
    margin-right: 1.25rem;
    display: flex;
    align-items: center;

    input {
      margin-right: .5rem;
    }
  }
`;